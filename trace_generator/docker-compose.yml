services:
  trace-gen-1:
    build:
      context: .
    image: trace-gen-1
    container_name: trace-gen-1
    volumes:
      - ./../benchmarks_new:/app/benchmarks
      - ./../trace_generator:/app/trace_generator
    stdin_open: true
    tty: true
    command: bash